package com.zywczas.commonutils.extensions

/**
 * @return just hour number, e.g. '14'
 */
//val Date.hour: String get() = SimpleDateFormat("HH", Locale.getDefault()).format(this)
//todo update to KMM, probably not needed of Long.toDate works with kotlinx date
